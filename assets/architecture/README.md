# Architecture

Runtime architecture and orchestration pipeline diagrams.

Expected assets:
- `runtime-architecture.png` — full system diagram (execution layer, cognitive runtime, n8n)
- `langgraph-pipeline.png` — node-level LangGraph decision pipeline
